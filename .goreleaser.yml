project_name: stowage

builds:
  - binary: stowage
    goos:
      - darwin
      - linux
    goarch:
      - amd64

archive:
  format: tar.gz
  replacements:
    amd64: 64-bit
    darwin: macOS
  files:
    - LICENSE
    - README.md
